package teste_ex03_ren;

public class Login {

    //atributos
    private static final String nome = "aluno";
    private static final String senha = "1234";

    //comportamento
    public void logar(String nomeUsuario, String senha) {
        if (this.nome.equals(nomeUsuario) && this.senha.equals(senha)) {

        }
    }
}
